# Number-Conversion
# Zahlensystem-Umwandlungen in Python

Dieses Python-Modul enthält Funktionen zur Umwandlung von Zahlen zwischen verschiedenen Zahlensystemen:  
Dezimal, Binär, Hexadezimal und Oktal.

## Funktionen

### Dezimal zu anderen Zahlensystemen

- `DezimalZuBinaer(n)`  
  Wandelt eine Dezimalzahl `n` in eine Binärzahl (als String) um.

- `DezimalZuHexadezimal(n)`  
  Wandelt eine Dezimalzahl `n` in eine Hexadezimalzahl (als String) um.

- `DezimalZuOktal(n)`  
  Wandelt eine Dezimalzahl `n` in eine Oktalzahl (als String) um.

### Andere Zahlensysteme zu Dezimal

- `BinaerZuDezimal(n)`  
  Wandelt eine Binärzahl `n` (als String oder int) in eine Dezimalzahl um.

- `HexZuDezimal(n)`  
  Wandelt eine Hexadezimalzahl `n` (als String, z.B. `"1A3F"`) in eine Dezimalzahl um.

- `OktalZuDezimal(n)`  
  Wandelt eine Oktalzahl `n` (als String oder int) in eine Dezimalzahl um.
